name: 🧰 Run Setup Script Automatically
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run setup script
        run: |
          chmod +x backend/scripts/setup.sh
          ./backend/scripts/setup.sh
